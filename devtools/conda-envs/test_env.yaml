name: menger_curvature-test
channels:
  - conda-forge
  - nodefaults
dependencies:
  # Base depends
  - python
  - pip

  # MDAnalysis
  - MDAnalysis

  # numba
  - numba

  # dask
  - dask

  # Testing
  - pytest
  - pytest-cov
  - pytest-xdist
  - codecov

  # Pip-only installs
  #- pip:
  #  - codecov


